# Secret Scanner 使用说明书

## 目录

1. [项目简介](#项目简介)
2. [功能特性](#功能特性)
3. [系统要求](#系统要求)
4. [安装指南](#安装指南)
5. [快速开始](#快速开始)
6. [详细使用说明](#详细使用说明)
7. [配置文件说明](#配置文件说明)
8. [内置检测规则](#内置检测规则)
9. [自定义检测规则](#自定义检测规则)
10. [CI/CD 集成](#cicd-集成)
11. [常见问题](#常见问题)
12. [项目结构](#项目结构)

---

## 项目简介

**Secret Scanner** 是一款 Python 开发的安全工具，用于扫描代码仓库中的敏感信息（如 API Key、密码、Token 等），生成详细报告并自动脱敏修复。

### 应用场景

- 代码提交前的安全检查
- CI/CD 流水线中的自动化安全扫描
- 代码审计和合规性检查
- 防止敏感信息泄露到版本控制系统

---

## 功能特性

| 功能 | 说明 |
|------|------|
| 本地目录扫描 | 递归扫描指定目录下的所有文件 |
| GitHub 远程扫描 | 通过 GitHub API 扫描用户/组织的所有仓库 |
| 内置规则引擎 | 12 种预定义敏感信息检测规则 |
| 自定义规则 | 支持通过 YAML 文件扩展检测规则 |
| Markdown 报告 | 生成结构化的扫描报告 |
| 自动脱敏 | 替换源文件中的敏感信息为占位符 |
| 安全存储 | 将提取的敏感信息安全保存到 `.env.secret` |
| CI/CD 集成 | GitHub Actions 工作流支持 |

---

## 系统要求

- **Python**: 3.8 或更高版本
- **操作系统**: Linux / macOS / Windows
- **依赖**: pip, git (用于远程扫描)

---

## 安装指南

### 1. 克隆项目

```bash
cd /mnt/d/wsl2/claudecode/.worktrees/secret-scanner
```

### 2. 安装依赖

```bash
# 安装运行依赖
pip install -e .

# 安装开发依赖（包含测试工具）
pip install -e ".[dev]"
```

### 3. 验证安装

```bash
# 查看帮助
python -m secret_scanner --help

# 查看版本
python -m secret_scanner --version
```

---

## 快速开始

### 扫描本地目录

```bash
# 基本用法
python -m secret_scanner scan --local /path/to/your/code

# 指定输出目录
python -m secret_scanner scan --local /path/to/your/code --output ./scan-results

# 扫描并自动脱敏
python -m secret_scanner scan --local /path/to/your/code --redact
```

### 扫描 GitHub 仓库

```bash
# 设置 GitHub Token
export GITHUB_TOKEN=your_github_token

# 扫描用户所有仓库
python -m secret_scanner scan --remote username --output ./scan-results

# 扫描并脱敏
python -m secret_scanner scan --remote username --redact --output ./scan-results
```

### 使用配置文件

```bash
python -m secret_scanner scan --config .secret-scanner.yaml
```

---

## 详细使用说明

### CLI 命令参数

```
$ python -m secret_scanner scan --help

Usage: python -m secret_scanner scan [OPTIONS]

  Scan repositories for secrets.

Options:
  -l, --local PATH        本地目录路径
  -r, --remote TEXT       GitHub 用户名
  -c, --config FILE       配置文件路径
  -o, --output DIR        输出目录
  --redact / --no-redact  是否脱敏源文件
  --github-token TEXT     GitHub API Token (或设置 GITHUB_TOKEN 环境变量)
  --help                  显示帮助信息
```

### 输出文件

扫描完成后会生成以下文件：

| 文件 | 说明 |
|------|------|
| `secret_report.md` | Markdown 格式的扫描报告 |
| `.env.secret` | 提取的敏感信息（仅在 `--redact` 模式下生成） |

### 报告格式示例

```markdown
# Secret Scan Report

**Generated:** 2026-03-31T12:00:00

## Summary

- **Total secrets found:** 5
- **High severity:** 2
- **Medium severity:** 3

## 🔴 High Severity

### AWS Access Key ID
- **Rule:** `aws_access_key`
- **Location:** `config.py:15`
- **Value:** `AKIA***EF`

### GitHub Token
- **Rule:** `github_token`
- **Location:** `.env:3`
- **Value:** `ghp_***xxxx`

## 🟡 Medium Severity

### Generic Password
- **Rule:** `generic_password`
- **Location:** `settings.py:42`
- **Value:** `pass***word`
```

### 脱敏格式

源文件中的敏感信息会被替换为以下格式：

```python
# 脱敏前
API_KEY = "sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# 脱敏后
API_KEY = "<REDACTED:sk-x...xxxx>"
```

---

## 配置文件说明

### 配置文件示例 (`.secret-scanner.yaml`)

```yaml
# 扫描配置
scan:
  mode: local  # local | remote | both
  github_token: ${GITHUB_TOKEN}  # 支持环境变量

# 输出配置
output:
  report_file: secret_report.md
  secret_storage: .env.secret

# 规则配置
rules:
  include_default: true  # 是否使用内置规则
  custom_rules: ./custom_rules.yaml  # 自定义规则文件路径

# 脱敏配置
redact: false  # 是否自动脱敏
```

### 配置选项说明

| 选项 | 说明 | 默认值 |
|------|------|--------|
| `scan.mode` | 扫描模式：local/remote/both | local |
| `scan.github_token` | GitHub API Token | 环境变量 |
| `output.report_file` | 报告文件名 | secret_report.md |
| `output.secret_storage` | 敏感信息存储文件名 | .env.secret |
| `rules.include_default` | 是否包含内置规则 | true |
| `rules.custom_rules` | 自定义规则文件路径 | null |
| `redact` | 是否自动脱敏 | false |

---

## 内置检测规则

| 规则 ID | 描述 | 严重程度 | 示例 |
|--------|------|---------|------|
| `aws_access_key` | AWS Access Key ID | High | `AKIAIOSFODNN7EXAMPLE` |
| `aws_secret_key` | AWS Secret Access Key | High | `aws_secret_access_key = xxx` |
| `github_token` | GitHub Token | High | `ghp_xxxxxxxxxxxx` |
| `gitlab_token` | GitLab Token | High | `glpat-xxxxxxxxxx` |
| `openai_api_key` | OpenAI API Key | High | `sk-xxxxxxxxxxxxxxxxxxxx` |
| `anthropic_api_key` | Anthropic API Key | High | `sk-ant-xxxxxxxxxxx` |
| `google_api_key` | Google API Key | High | `AIzaXXXXXXXXXXXX` |
| `private_key` | 私钥文件 | High | `-----BEGIN RSA PRIVATE KEY-----` |
| `database_url` | 数据库连接字符串 | High | `mysql://user:pass@host/db` |
| `generic_password` | 通用密码 | Medium | `password = "xxxx"` |
| `generic_api_key` | 通用 API Key | Medium | `api_key = "xxxx"` |
| `jwt_secret` | JWT 密钥 | Medium | `jwt_secret = "xxxx"` |

---

## 自定义检测规则

### 创建自定义规则文件

```yaml
# custom_rules.yaml
rules:
  - id: company_api_key
    pattern: "company_[a-z]+_key\\s*[=:]\\s*[\"'][A-Za-z0-9]{16,}[\"']"
    description: "公司内部 API Key"
    severity: high

  - id: slack_webhook
    pattern: "https://hooks\\.slack\\.com/services/T[A-Z0-9]+/B[A-Z0-9]+/[A-Za-z0-9]+"
    description: "Slack Webhook URL"
    severity: medium

  - id: stripe_key
    pattern: "sk_live_[0-9a-zA-Z]{24}"
    description: "Stripe Live API Key"
    severity: high
```

### 在配置文件中引用

```yaml
rules:
  include_default: true
  custom_rules: ./custom_rules.yaml
```

### 规则定义格式

| 字段 | 说明 | 必填 |
|------|------|------|
| `id` | 规则唯一标识符 | 是 |
| `pattern` | 正则表达式 | 是 |
| `description` | 规则描述 | 是 |
| `severity` | 严重程度：high/medium/low | 是 |

---

## CI/CD 集成

### GitHub Actions 工作流

创建 `.github/workflows/scan.yml`:

```yaml
name: Secret Scan

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install secret-scanner
        run: |
          pip install -e .

      - name: Run secret scan
        run: |
          python -m secret_scanner scan \
            --local . \
            --output . \
            --config .secret-scanner.yaml || true

      - name: Upload report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: secret-scan-report
          path: |
            secret_report.md
            .env.secret
```

### GitLab CI/CD

```yaml
# .gitlab-ci.yml
secret_scan:
  image: python:3.10
  script:
    - pip install -e .
    - python -m secret_scanner scan --local . --output .
  artifacts:
    paths:
      - secret_report.md
    when: always
```

---

## 常见问题

### Q1: 如何排除某些目录不被扫描？

在调用扫描时，工具会自动排除以下目录：
- `__pycache__`, `.git`, `node_modules`
- `*.pyc`, `*.pyo`
- `.venv`, `venv`, `dist`, `build`

如需添加更多排除，可在代码中修改 `default_excludes` 列表。

### Q2: 误报了怎么办？

1. 检查自定义规则的正则表达式是否过于宽泛
2. 可以在扫描后手动确认报告内容
3. 提交 issue 反馈误报规则

### Q3: 如何更新内置规则？

编辑 `src/secret_scanner/rules/default_rules.yaml` 文件，添加或修改规则。

### Q4: GitHub Token 如何获取？

1. 访问 https://github.com/settings/tokens
2. 生成新的 Personal Access Token
3. 至少需要 `repo` 权限（用于访问私有仓库）
4. 将 Token 设置为环境变量：`export GITHUB_TOKEN=xxx`

### Q5: 脱敏后如何恢复原始文件？

脱敏操作会直接修改源文件。建议：
1. 扫描前先提交代码到 git
2. 如需恢复，使用 `git checkout` 还原文件
3. 敏感信息保存在 `.env.secret` 文件中

---

## 项目结构

```
secret-scanner/
├── src/secret_scanner/       # 源代码目录
│   ├── __init__.py           # 包初始化
│   ├── cli.py                # 命令行入口
│   ├── scanner.py            # 核心扫描器
│   ├── detector.py           # 检测引擎
│   ├── redactor.py           # 脱敏器
│   ├── reporter.py           # 报告生成器
│   ├── storage.py            # 安全存储
│   ├── github_client.py      # GitHub API 客户端
│   └── rules/
│       ├── __init__.py
│       ├── engine.py         # 规则引擎
│       └── default_rules.yaml # 内置规则
├── tests/                    # 测试文件
│   ├── test_cli.py
│   ├── test_detector.py
│   ├── test_redactor.py
│   ├── test_reporter.py
│   ├── test_rules.py
│   ├── test_scanner.py
│   └── test_storage.py
├── .github/workflows/
│   └── scan.yml              # GitHub Actions 配置
├── pyproject.toml            # Python 项目配置
├── .secret-scanner.yaml      # 示例配置文件
└── README.md                 # 项目说明文档
```

---

## 运行测试

```bash
# 运行所有测试
pytest tests/ -v

# 生成覆盖率报告
pytest tests/ -v --cov=secret_scanner --cov-report=html

# 运行单个测试文件
pytest tests/test_detector.py -v
```

---

## 版本历史

- **v0.1.0** (2026-03-31)
  - 初始版本发布
  - 支持本地和远程扫描
  - 12 种内置检测规则
  - Markdown 报告生成
  - 自动脱敏功能
  - GitHub Actions 集成

---

## License

MIT License

---

## 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本项目
2. 创建功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到远程 (`git push origin feature/amazing-feature`)
5. 创建 Pull Request
